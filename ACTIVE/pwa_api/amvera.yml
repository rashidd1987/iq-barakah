---
meta:
  environment: python
  toolVersion: "3.11"
  projectName: iq-barakah-pwa-api

build:
  pip: requirements.txt

run:
  persistenceMount: /data
  command: uvicorn main:app --host 0.0.0.0 --port 8080
