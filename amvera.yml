meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: ACTIVE/pwa_api/requirements.txt
  useCache: false

run:
  command: bash start_pwa.sh
  persistenceMount: /data
  httpPort: 80
