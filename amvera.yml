meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: ACTIVE/pwa_api/requirements.txt
  useCache: false

run:
  command: python pwa_run.py
  persistenceMount: /data
  httpPort: 80
