---
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11
build:
  requirementsPath: report_bot/requirements.txt
  useCache: true
run:
  persistenceMount: /data
  command: python -m report_bot.main
  containerPort: 8080
