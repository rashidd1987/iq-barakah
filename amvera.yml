meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: bot/requirements.txt
  useCache: true

run:
  command: python bot/bot.py
  persistenceMount: /data
