meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: ACTIVE/pwa_api/requirements.txt
  useCache: false

run:
  command: bash -c "cd ACTIVE/pwa_api && python -m uvicorn main:app --host 0.0.0.0 --port 80"
  persistenceMount: /data
  httpPort: 80
